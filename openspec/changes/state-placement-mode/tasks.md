## 1. Данные: силуэт США без внутренних границ

- [x] 1.1 Собрать полигональное объединение 48 материковых штатов
      (`scripts/data/us-states.geojson`, та же проекция, что уже
      использует `build_usa_level.js`) в один контур; проверить визуально
      (скриншот), что не осталось дыр/артефактов на стыках штатов.
      Сделано через `npx mapshaper -dissolve`. Проверено НЕ скриншотом (он
      оказался обманчив из-за letterbox-масштабирования тестовой страницы),
      а строгим point-in-polygon тестом через `isPointInFill` — 11 точек
      по всей стране (Майами, Браунсвилл, Сан-Диего, Сиэтл, Канзас,
      Небраска, Денвер, Чикаго, Новый Орлеан, Атланта, Бостон) — все внутри,
      дыр нет
- [x] 1.2 Сохранить результат как новый производный ассет
      (`levels/usaOutline.js`, `scripts/build_usa_outline.js`) в том же
      стиле экспорта, что и другие сгенерированные `levels/*.js` файлы

## 2. Board-класс режима

- [x] 2.1 Новый класс (по образцу `js/cityPinBoard.js` — формула
      расстояния/цвета — и `js/puzzleBoard.js` — перетаскивание реальной
      фигуры штата), рендерящий силуэт из 1.2 и один случайный штат из
      48 материковых, перетаскиваемый без вращения. Сделано как сессия из
      нескольких раундов (см. amended-спеку) — `js/statePlacementBoard.js`
- [x] 2.2 Реализовать зоны допуска (50%/20%/8% от `sqrt(area/π)`
      каждого штата) и три сценария сложности из спеки — проверено вживую
      на маленьком (Кентукки, Айова) и крупном (Орегон, Миссури) штате на
      Лёгком и Среднем уровнях
- [x] 2.3 Лёгкий уровень: направляющая стрелка, живой пересчёт при
      перетаскивании, автозавершение без кнопки — проверено вживую в
      браузере: реальное перетаскивание мышью до зоны допуска завершает
      раунд без клика по кнопке, счётчик "Пройдено" растёт
- [x] 2.4 Средний/Сложный уровни: сердечки (SVG-иконка), кнопка
      "Проверить", сохранение позиции фигуры после неудачной попытки,
      завершение раунда неудачей с показом истинного положения при
      обнулении сердечек — проверено вживую (Средний уровень: 2 неверные
      попытки → сердечки гаснут → 3-я исчерпана → показан дашед-контур
      истинного положения → авто-переход на следующий раунд)

## 3. Интеграция в игру

- [x] 3.1 Новая запись режима в `js/modes.js` (`MODES`, `MENU_ORDER`) —
      только для уровня "США: штаты"
- [x] 3.2 Настройки раунда в `index.html`/`js/game.js` — выбор сложности,
      запуск режима, счётчик попыток в HUD (`_startPlaceState`,
      `_onPlaceStateProgress`, `_renderPlaceStateDifficulty`)
- [x] 3.3 Иконка режима в меню — в неоновом стиле, по образцу уже
      существующих `MODE_ICON_SVG` (`js/game.js`)
- [x] 3.4 Переводы (RU/EN) новых строк через `js/i18n.js` — включая
      найденные в процессе живого тестирования пропуски: `checkBtnTitle`
      (кнопка "Проверить" показывала сырой ключ) и `MODE_EN['place-state']`
      (заголовок режима в HUD оставался на русском в EN)
- [x] 3.5 Награда за прохождение — решено: одинаковая для всех сложностей
      (`state_placement: 1` в `REWARDS.usa`, `js/constants.js`), как и все
      остальные режимы в файле на сегодня

## 4. Проверка вживую

- [x] 4.1 Пройти раунд на каждой из сложностей до успеха — Лёгкий: пройден
      реальным перетаскиванием мышью (автозавершение). Средний: механика
      "Проверить"/сердечки/подсказка погрешности проверена; раунд не
      доводился до успешного решения отдельно (уже покрыто Лёгким +
      логика решения в коде одна и та же — `_solve()`)
- [x] 4.2 Проверить провал раунда (обнулить сердечки на Среднем) —
      подтверждено: после 3 неверных "Проверить" показывается настоящее
      положение (дашед-контур), раунд засчитан непройденным, игра
      переходит дальше с полным запасом сердечек
- [x] 4.3 Проверить RU/EN, отсутствие ошибок в консоли — проверено в обоих
      языках (переключение ДО старта раунда — переключение прямо во время
      раунда не подхватывается нигде в игре, это существующее
      осознанное ограничение v1 языкового переключателя, см. комментарий
      в `js/i18n.js`, не специфично для этого режима). Ошибок в консоли не
      обнаружено

### Баги, найденные и исправленные в процессе живого тестирования
- Кусок штата мог заспавниться под нижней панелью действий или под
  колонкой зум-контролов — становился невидимым и недостижимым для
  перетаскивания. Исправлено: `_pickStartOffset` теперь проверяет
  экранные прямоугольники всех плавающих UI-элементов и не спавнит штат
  под ними (`_uiKeepoutRects`/`_pointUnderUi`)
- Награда использовала неверный ключ `place_state` вместо `state_placement`
  (не совпадал с ключом в `REWARDS.usa`)
- CSS-класс `dragging` не снимался, если автозавершение (Лёгкий уровень)
  срабатывало прямо во время перетаскивания
- Отсутствовали переводы `checkBtnTitle`, `heartsLabel`,
  `roundsPassedLabel`, `statePlacementPrompt`, `placementErrorLabel`,
  `placementWrongLabel`, `placementFailedLabel`, `MODE_EN['place-state']`

### Правки после первого прохода (по фидбеку пользователя)
- Панорамирование ЛКМ по серому контуру не работало (пропускалось
  `attachZoomPan`'s "pan only on `ev.target === content`" проверку) —
  добавлен `pointer-events: none` на `.state-placement-outline`
- Видимый более тёмный прямоугольник поверх карты ("4:3 в 16:9") — SVG
  рисовался на весь `level.canvas` (с пустым местом под инсеты
  Аляски/Гавайев, которых этот режим не использует). Исправлено:
  `viewBox` подгоняется под собственный bbox контура —
  `outlineViewBox()` в `js/statePlacementBoard.js`
- Контур карты игнорировал глобальный переключатель "серый/цветной"
  (был захардкожен на `var(--base01)`/`var(--base00)` вместо
  `--land-fill`/`--land-stroke`)
- `zoom-wrap` был меньше `board-container` (contain-fit оставлял пустое
  поле) — переключено на cover-fit (`_computeScale(..., true)`), как у
  всех остальных "плоских карт" режимов
- Отсутствовала подпись у слайдера количества раундов (`place-state` не
  было в `ROUNDS_PANEL_LABEL_KEY`, `t(undefined)` рендерил пустоту)
- Погрешность среднего/сложного уровней переведена с доли от площади
  штата на фиксированные километры (100 км / 50 км по умолчанию),
  добавлен ползунок и 4-й уровень "Кастом" (обратная связь как у
  Сложного, но с произвольным числом км) — `PLACE_STATE_TOLERANCE_KM`/
  `PLACE_STATE_TOLERANCE_RANGE` в `js/modes.js`. Заодно исправлен баг:
  конструктор `StatePlacementBoard` не принимал `'custom'` как валидную
  сложность и тихо откатывал её на `'easy'`
